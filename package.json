{
  "name": "polis-prediction-markets",
  "version": "1.0.0",
  "description": "POLIS - The Autonomous Prediction Market Collective",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:flare": "npx hardhat run scripts/deploy-flare.js --network coston2",
    "deploy:plasma": "npx hardhat run scripts/deploy-plasma.js --network plasmaTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
